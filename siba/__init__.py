"""Núcleo SIBA — geração de boletins de alojamento (XML BAL.XSD e ficheiro .DAT)
e submissão pelo web service oficial WSSIBA.

Só usa biblioteca-padrão do Python, por isso a geração de XML/.DAT funciona
sempre — mesmo sem dependências instaladas. Esta é a base do sistema de fallback.
"""
